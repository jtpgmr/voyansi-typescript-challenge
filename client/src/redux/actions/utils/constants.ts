export enum RoomActionType {
  FETCH_ALL = "FETCH_ALL",
  UPDATE_OCCUPANT = "UPDATE_OCCUPANT",
  REMOVE_OCCUPANT = "REMOVE_OCCUPANT",
}

export enum AuthActionType {
  AUTH = "AUTH",
  LOGOUT = "LOGOUT",
}